---
// Technology names are proper names, so the list carries no translated text.
interface Props {
  items: readonly string[];
}
const { items } = Astro.props;
---

<ul class="tags">
  {items.map((item) => (
    <li>{item}</li>
  ))}
</ul>
