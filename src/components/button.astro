---
import PixelIcon, { type IconName } from '@/components/pixel-icon.astro';
// Two forms from the design master. `solid`: a sharp ink rectangle with a
// square bullet; on hover the accent slides in behind, the label steps left
// and the arrow appears. `line`: a bottom rule whose accent underline grows.
interface Props {
  href: string;
  label: string;
  variant?: 'solid' | 'line';
  icon?: IconName;
  rel?: string;
}
const {
  href,
  label,
  variant = 'solid',
  icon = 'arrow-right',
  rel,
} = Astro.props;
---

{variant === 'solid' ? (
  <a class="button" href={href} rel={rel} data-scramble-hover>
    <span class="button-dot" aria-hidden="true" />
    <span class="button-text-wrap">
      <span class="button-text" data-scramble-text>
        {label}
      </span>
      <PixelIcon name={icon} class="button-icon" />
    </span>
    <span class="button-bg" aria-hidden="true" />
  </a>
) : (
  <a class="button-line" href={href} rel={rel} data-scramble-hover>
    <PixelIcon name={icon} class="button-line-icon" />
    <span data-scramble-text>{label}</span>
    <span class="button-line-bar" aria-hidden="true" />
  </a>
)}

<style>
  .button,
  .button-line {
    position: relative;
    isolation: isolate;
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 3rem;
    font-family: var(--font-mono);
    text-transform: uppercase;
    overflow: hidden;
  }
  .button {
    gap: 1rem;
    min-width: 10rem;
    padding: 0.5rem 1rem;
    background: var(--ink);
    color: var(--paper);
    font-weight: 600;
    transition: color 0.2s ease;
  }
  .button-dot {
    flex: none;
    width: 0.5rem;
    aspect-ratio: 1;
    background: currentColor;
  }
  .button-text-wrap {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }
  .button-text {
    position: relative;
    right: -1.5rem;
    transition: right 0.2s ease;
  }
  .button :global(.button-icon) {
    flex: none;
    width: 1.25rem;
    height: 1.25rem;
    padding: 0.2rem;
    opacity: 0;
    transition: opacity 0.2s ease;
  }
  .button-bg {
    position: absolute;
    inset: 0;
    z-index: -1;
    background: var(--accent);
    transform: translateX(-100%);
    transition: transform 0.2s ease;
  }
  .button:hover,
  .button:focus-visible {
    color: var(--accent-contrast);
  }
  .button:hover .button-bg,
  .button:focus-visible .button-bg {
    transform: none;
  }
  .button:hover .button-text,
  .button:focus-visible .button-text {
    right: 0;
  }
  .button:hover :global(.button-icon),
  .button:focus-visible :global(.button-icon) {
    opacity: 1;
  }

  .button-line {
    gap: 2rem;
    padding-block: 0.5rem;
    border-bottom: 1px solid var(--ink);
    color: var(--ink);
    font-weight: 500;
    overflow: visible;
    transition: color 0.2s ease;
  }
  .button-line :global(.button-line-icon) {
    flex: none;
    width: 1rem;
    height: 1rem;
  }
  .button-line-bar {
    position: absolute;
    inset: auto auto -1px 0;
    width: 0;
    height: 4px;
    background: var(--accent);
    transition: width 0.2s ease;
  }
  .button-line:hover,
  .button-line:focus-visible {
    color: var(--accent);
  }
  .button-line:hover .button-line-bar,
  .button-line:focus-visible .button-line-bar {
    width: 100%;
  }
  @media (prefers-reduced-motion: reduce) {
    .button,
    .button-text,
    .button :global(.button-icon),
    .button-bg,
    .button-line,
    .button-line-bar {
      transition: none;
    }
  }
</style>
